// ignore_for_file: dangling_library_doc_comments

/// Replace page_template with the name of the
/// file that you have created.
export 'page_template.dart';

import 'package:flutter/material.dart';

/// Replace page_name with the name of your new
/// page. Replace ListView() with the widget 
/// you want to use. Only one widget can be 
/// returned so all your page content must be 
/// contained within it. I recommend using 
/// ListView() to maintain conceptual integrity 
/// across the project.
class page_name extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
